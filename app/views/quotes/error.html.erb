<% if flash[:error].present? %>
	<div class="col-xs-10 col-xs-offset-1 alert alert-danger", data-toggle="modal", data-target="#myModal">
		<%= flash[:error].html_safe %>
	</div>
<% end %>
<h2>Make sure both fields are filled in, and your quote (and your name) isn't too short!</h2>